# Vehicle meshes (non-voxel)

Primary: procedural kits from `scripts/vehicles/procedural_vehicle.gd`
(body / glass / lights / wheels assigned at build time).

Catalog: `catalog.json` (`source: procedural`). Kenney Car Kit GLBs under `models/`
remain as `fallback_path` only (opaque last resort — no transparency heuristics).

See `CREDITS.txt` and root `LICENSE_ASSETS.md`.
